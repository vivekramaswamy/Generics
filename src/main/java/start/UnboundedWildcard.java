package start;

/**
 * ? is called the unbounded wild card. As when we use it, we have no idea on its type.
 * For example if I make a List<?> what I am telling the compiler is that I don't know what the
 * underlying type is. You can read from it, but you can't write to it.
 */
public class UnboundedWildcard {

    public static void main(String[] args) {


    }
}
